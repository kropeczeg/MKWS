# MKWS 2026 project -- Python dependencies
#
# Tested with Python 3.11 / 3.12.
# Install with:   pip install -r requirements.txt
#
# Cantera also ships as a conda package (conda install -c cantera cantera),
# which is the route recommended by the course materials.

cantera>=3.0
numpy>=1.24
matplotlib>=3.7

# For the notebook only:
pandas>=2.0
jupyter>=1.0
nbformat>=5.0
